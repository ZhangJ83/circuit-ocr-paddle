\documentclass{article}

\usepackage{arxiv}

\usepackage{iftex}

\ifPDFTeX
  \usepackage[utf8]{inputenc}
  \usepackage[T1]{fontenc}
\else
  \usepackage{fontspec}
  \setmainfont{Times New Roman}
  \setsansfont{Arial}
  \setmonofont{Courier New}
\fi

\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{natbib}
\usepackage{doi}
\usepackage{float}

\extrafloats{30}
\usepackage{placeins}

\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric, calc}

\usepackage{caption}
\usepackage{cleveref}

\captionsetup{font=small,labelfont=bf,skip=4pt}
\setlist{nosep,leftmargin=*}

\title{PaddleOCR-VL-Circuit: Built for Schematic Diagram Understanding}

\newif\ifuniqueAffiliation
\uniqueAffiliationtrue

\ifuniqueAffiliation
\author{ Jianning Zhang \\
    School of Flexible Electronics\\
    Sun Yat-sen University\\
    Shenzhen, Guangdong, China \\
    \texttt{zhangjn83@mail2.sysu.edu.cn} \\
    \And
    Yifei Chen \\
    School of Computer Science\\
    Sun Yat-sen University\\
    Guangzhou, Guangdong, China \\
    \texttt{chenyf376@mail2.sysu.edu.cn} \\
}
\else
\usepackage{authblk}
\renewcommand\Authfont{\bfseries}
\setlength{\affilsep}{0em}
\newbox{\orcid}\sbox{\orcid}{\includegraphics[scale=0.06]{orcid.pdf}}
\author[1]{%
    \href{https://orcid.org/0000-0000-0000-0000}{\usebox{\orcid}\hspace{1mm}Jianning Zhang\thanks{\texttt{zhangjn83@mail2.sysu.edu.cn}}}%
}
\author[2]{%
    \href{https://orcid.org/0000-0000-0000-0000}{\usebox{\orcid}\hspace{1mm}Yifei Chen\thanks{\texttt{chenyf376@mail2.sysu.edu.cn}}}%
}
\affil[1]{School of Flexible Electronics, Sun Yat-sen University, Shenzhen, China}
\affil[2]{School of Computer Science, Sun Yat-sen University, Guangzhou, China}
\fi

\renewcommand{\headeright}{}
\renewcommand{\undertitle}{}
\renewcommand{\shorttitle}{PaddleOCR-VL-Circuit}

\hypersetup{
pdftitle={PaddleOCR-VL-Circuit: Built for Schematic Diagram Understanding},
pdfsubject={Circuit Schematic OCR, Visual Language Model, LoRA Fine-tuning},
pdfauthor={Jianning Zhang, Yifei Chen},
pdfkeywords={Circuit Schematic, OCR, PaddleOCR-VL, LoRA, Netlist Extraction},
}

\date{}

\begin{document}
\maketitle

\begin{abstract}
Automatic recognition of circuit schematic diagrams is a core challenge in Electronic Design Automation (EDA). This technical report presents CircuitOCR, a system based on PaddleOCR-VL-0.9B for circuit schematic OCR and netlist extraction. We construct the V5 Golden dataset of 1,857 schematics (500 synthetic + 1,357 real KiCad projects) with 100\% visual-literal alignment. Through systematic LoRA ablation experiments, we identify the root cause of modality collapse---fine-tuning the vision-language projector destroys pre-trained alignment---and introduce the LLM-Only LoRA strategy that resolves it. The V10-Fixed model uses wide LoRA ($r=16$, $\alpha=32$, 5.7M params), fixes six critical training bugs---three in sequence generation (causal token double-shift, BPE boundary merging, \texttt{set\_state\_dict} silent failure in Paddle 3.1.0) and three PaddleOCR-VL-specific (LoRA precision loss, tokenizer offset, gradient accumulation decoupling; see dataset README), and trains for 3 epochs (1,165 steps) in 43 minutes on an RTX 4060 (8\,GB). Phase 1 evaluation on easy50-pure (44 samples) yields Component F1 = 0.2061 (4.5$\times$ over baseline 0.0455) and Token Recall = 0.1540 (96$\times$ over baseline 0.0016). Phase 2 topology evaluation reveals the honest capability: joint (refdes, value) pair F1 = 0.019---only $\sim$2\% of components are fully correct, with 87\% of values hallucinated. All code, weights, datasets, and a live demo are open-sourced. Detailed experimental results, annotated examples, and version history are provided in the Appendix.
\end{abstract}

\keywords{Circuit Schematic \and OCR \and PaddleOCR-VL \and LoRA Fine-tuning \and Netlist Extraction \and Modality Collapse}


\section{Introduction}

Vision-Language Models (VLMs) can directly output structured text from images end-to-end, but their application to circuit schematic diagrams remains unexplored. General-purpose OCR engines such as Tesseract~\citep{smith2007tesseract} and PaddleOCR~\citep{du2020ppocr} are designed for natural-scene and document text, not engineering schematics. VLM-based document understanding systems like Donut~\citep{kim2022donut} and Pix2Struct~\citep{lee2023pix2struct} demonstrate OCR-free parsing for general documents, while general-purpose VLMs (LLaVA~\citep{liu2023llava}, Qwen-VL~\citep{bai2023qwen}, InstructBLIP~\citep{dai2023instructblip}) have not been evaluated on circuit diagrams. No dedicated public benchmark exists for circuit schematic OCR. Existing datasets like Open Schematics~\citep{openschematics2025} lack standardized evaluation protocols, while textbook-derived datasets suffer from SPICE-induced visual-to-logic annotation discrepancies.

The industrial need is substantial. The global semiconductor market exceeded \$600 billion in 2025 (SEMI/WSTS). PCB reverse engineering outsourcing alone exceeds \$500 million annually, with approximately 30\% of time spent on schematic reconstruction. Use cases include hardware reverse engineering, legacy document digitization (1980s--2000s paper schematics), cross-tool migration, and the 500,000+ KiCad/EAGLE projects on GitHub lacking structured netlist exports.

Circuit schematic understanding involves two heterogeneous sub-tasks: (1) visual text recognition---extracting component designators (R1, C1), values (10k, 100nF), and net labels (VCC, GND); (2) topology graph tracing---inferring electrical connectivity from wires and net labels. Our evaluation protocol measures both dimensions: text NED for explicit recognition and component F1 for implicit structural understanding.


\section{System Setup}

We adopt PaddleOCR-VL-0.9B~\citep{cui2025paddleocrvl} (908M params), which integrates a NaViT-style dynamic vision encoder with the ERNIE-4.5-0.3B language model, supporting 109 languages. The unfine-tuned base model fails systematically on circuit schematics: it outputs generic document text (e.g., parameter tables), memorized hallucinations, or repetitive token sequences. These failures stem from pre-training data dominated by general documents with negligible exposure to engineering drawings.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    node distance=0.55cm,
    frozen/.style={rectangle, draw=blue!55!black, fill=blue!8, very thick, minimum width=4.4cm, minimum height=0.8cm, rounded corners=3pt, align=center, font=\small},
    train/.style={rectangle, draw=orange!80!black, fill=orange!12, very thick, minimum width=4.4cm, minimum height=0.8cm, rounded corners=3pt, align=center, font=\small},
    io/.style={rectangle, draw=gray!55!black, fill=gray!8, thick, minimum width=3.6cm, minimum height=0.65cm, rounded corners=3pt, align=center, font=\small},
    arr/.style={-Stealth, thick, gray!60!black},
    eblab/.style={font=\scriptsize, text=gray!50!black}
]
\node[io] (img) {Input Schematic (384\,px)};
\node[frozen, below=of img] (ve) {NaViT Vision Encoder \scriptsize(frozen)};
\node[frozen, below=of ve] (proj) {Projector \texttt{mlp\_AR} (25.9M) \scriptsize(frozen)};
\node[train, below=of proj] (llm) {ERNIE-4.5-0.3B LLM \scriptsize(LoRA $r{=}16$, 5.7M, 0.63\%)};
\node[io, below=of llm] (out) {Netlist Text Output};
\node[io, left=1.4cm of llm] (prompt) {Text Prompt};
\draw[arr] (img) -- (ve);
\draw[arr] (ve) -- node[eblab, right] {visual tokens} (proj);
\draw[arr] (proj) -- node[eblab, right] {lang.\ embed.} (llm);
\draw[arr] (llm) -- (out);
\draw[arr] (prompt) -- (llm);
\end{tikzpicture}
\caption{LLM-Only LoRA architecture. Blue modules are frozen; the orange LLM module receives low-rank adapters. The Projector (\texttt{mlp\_AR}) is the sole visual-to-language bridge---perturbing it causes modality collapse (E4, Section~4.1)---so it is kept frozen. Only LLM self-attention and linear projection matrices (310 targets) are adapted.}
\label{fig:architecture}
\end{figure}


\section{V5 Golden Dataset}

\subsection{Legacy Issues and Design Principles}

Earlier dataset versions (V1--V4) had three fatal flaws: (1) GT-image misalignment---labels from KiCad S-expression parsing included invisible pin numbers and hidden net names; (2) format mismatch---Open Schematics subsets used horizontal space-concatenated format (62.1\% single-word lines vs.\ test set's 98.6\%); (3) duplication noise---22,340 intra-line duplicate word pairs contaminating 24.3\% of samples.

V5 Golden was rebuilt on two principles: (1) GT must align 100\% with visible on-image text, using \texttt{draw.text()} synchronous recording for synthetic data and SVG stroked-text extraction for real KiCad data, with zero AI-generated labels; (2) format must match the test set, replacing mismatched subsets with train-real data (97.9\% single-word lines), reducing duplication noise by 99.5\% (22,340 $\to$ 107).

\subsection{Data Sources and Statistics}

\textbf{Synthetic V3 (500 images).} Random circuit topologies (5--30 components, 10 types) rendered as PNG, with labels from \texttt{draw.text()} logs~\citep{zhang2026circuitocr}.

\textbf{train-real (1,357 images).} Real open-source KiCad projects from GitHub, rendered via kicad-cli SVG $\to$ cairosvg PNG, with GT from SVG stroked-text visible elements in one-word-per-line vertical format.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/dataset_overview.png}
\caption{Dataset overview: three data sources. \textbf{Top row}---Synthetic V3: programmatically generated with randomized topologies (5--30 components, 10 types), GT recorded synchronously via \texttt{draw.text()}. \textbf{Middle row}---train-real (KiCad): real open-source projects from GitHub, rendered via kicad-cli SVG$\to$cairosvg PNG, GT extracted from SVG stroked-text. \textbf{Bottom row}---Test set (easy50-pure): held-out real schematics for evaluation. All images are raw PNG renders with zero AI-generated labels.}
\label{fig:dataset_overview}
\end{figure}

\begin{table}[h]
\centering
\caption{Dataset Composition and Split}
\label{tab:dataset}
\begin{tabular}{lcccc}
\toprule
\textbf{Split} & \textbf{Synthetic V3} & \textbf{train-real} & \textbf{Total} & \textbf{avg Lines} \\
\midrule
Training   & 450 & 1,104 & 1,554 & 32.1 \\
Validation & 50  & 171   & 221   & 33.4 \\
Test (easy50) & -- & --  & 44    & -- \\
Test (easy100) & -- & -- & 89   & -- \\
\midrule
\textbf{Total} & \textbf{500} & \textbf{1,357} & \textbf{1,857} & -- \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent The 82-sample gap between the Total row (1,857) and the sum of Training + Validation rows (1,775) consists of real KiCad samples held out as an additional clean test set, separate from the easy50/easy100 benchmark splits. The V5 Golden total is thus 1,857 = 1,554 (train) + 221 (val) + 82 (held-out test).}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.80\textwidth]{../circuit-ocr-dataset/figures/dataset_donut.png}
\caption{Dataset composition. Left: full V5 Golden breakdown (1,857 total). Center: training set dominated by real KiCad projects (71\%). Right: validation set follows a similar distribution. Synthetic V3 data is the minority in all splits (29\% train, 23\% val), yet its influence on model behavior is disproportionate (see Section~7.3).}
\label{fig:dataset_donut}
\end{figure}

During training, we apply online augmentations: random JPEG compression (quality 85--100), random scaling (max\_dim 168--384px), and random horizontal flipping.


\section{Model Fine-Tuning}

\subsection{Modality Collapse: Discovery and Root Cause}

Initial full LoRA experiments ($r=8$, q/k/v/o self-attention, 2.75M params on 2,433 samples) reduced NED from 0.8848 to 0.8554, but output diversity collapsed to 4\%---nearly all test samples produced identical output. We define this as \textbf{Modality Collapse}.

Through six controlled experiments (E1--E6) summarized in Table~\ref{tab:ablation}, we systematically identified the root cause:

\begin{table}[h]
\centering
\caption{Ablation Experiments for Modality Collapse}
\label{tab:ablation}
\begin{tabular}{lcp{3cm}p{3cm}}
\toprule
\textbf{Exp} & \textbf{Variable} & \textbf{Result} & \textbf{Conclusion} \\
\midrule
E1 & Real vs.\ blank image & Outputs differ & Vision encoder works \\
E2 & Resolution 168$\to$336px & Still collapses & Resolution not the cause \\
E3 & 1 vs.\ 3 epochs & NED improves 0.9\% & Multi-epoch helps but insufficient \\
E4 & Add Projector LoRA & NED drops to 0.8271 & \textbf{Projector is the bottleneck} \\
E5 & Rank $r=8\to r=16$ & NED drops to 0.7961 & Capacity helps, diversity still 4\% \\
E6 & Freeze Projector & Diversity 90\% & \textbf{Collapse fully resolved} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Core finding: Projector LoRA is the root cause.} The vision-language projector (\texttt{mlp\_AR}, 25.9M params) is the sole bridge from visual features to language space. LoRA perturbation distorts these features into out-of-distribution vectors the LLM cannot interpret, causing it to degenerate into repeating high-frequency training tokens. Freezing the Projector while teaching only the LLM to reinterpret existing visual features is the safe strategy.

\subsection{V5: LLM-Only LoRA Validation}

V5 freezes the Projector and fine-tunes only LLM self-attention ($r=8$, $\alpha=16$, 1.25M params), with resolution increased to 384px and learning rate $2\times10^{-5}$. Training on 1,857 samples for 1 epoch (928 steps, 22 min) achieved 90\% output diversity---the first fine-tuned model to produce different outputs for different images. However, $r=8$ capacity was insufficient: NED (0.9031) remained below baseline due to premature convergence at $\sim$200 steps.

\subsection{V10-Fixed: Six Technical Fixes}

V10-Fixed scales LoRA to $r=16$, $\alpha=32$ (5.7M params, 0.63\% of 908M), targeting 310 projection matrices across LLM self-attention and linear layers. LoRA~\citep{hu2021lora} augments a frozen weight $W_0 \in \mathbb{R}^{d \times k}$ with a low-rank update $\Delta W = BA$, where $B \in \mathbb{R}^{d \times r}$, $A \in \mathbb{R}^{r \times k}$, and $r \ll \min(d, k)$. The forward pass becomes $h = W_0 x + \tfrac{\alpha}{r} BAx$, with $W_0$ frozen and only $A, B$ trained. Six critical training bugs were fixed (three additional PaddleOCR-VL-specific infrastructure bugs are documented in the dataset README):

\begin{enumerate}[nosep]
    \item \textbf{Causal token double-shift}: PaddleOCR-VL's \texttt{AutoModelForConditionalGeneration} internally applies causal shift. Our manual shift caused double-shifting---supervising step $t$ with label $t+2$, corrupting gradients. Fixed by removing the manual shift.
    \item \textbf{BPE boundary merging}: Concatenating prompt and label strings before tokenization causes BPE to merge characters across the boundary (e.g., ``\texttt{\textbackslash n}'' + ``R'' $\to$ merged token). Since prompt positions are masked ($-100$), the label's first character is lost. Fixed by tokenizing prompt and label independently and concatenating token IDs.
    \item \textbf{\texttt{set\_state\_dict} silent failure}: In Paddle 3.1.0, \texttt{LoRAModel.set\_state\_dict()} returns \texttt{None} instead of a tuple, causing silent LoRA loading failure. Fixed by using the \texttt{LoRAModel} wrapper with manual \texttt{p.set\_value()} per parameter.
\end{enumerate}

These six bugs are implementation pitfalls we document for community benefit---they affect all PaddleOCR-VL fine-tuning workflows that follow standard practices. The three sequence-generation bugs above affect any VLM fine-tuning pipeline. The three additional PaddleOCR-VL-specific bugs---(4) LoRA weight precision loss (\texttt{p.set\_value()} float32$\to$float16 truncation), (5) tokenizer special token offset (coordinate prediction bias), and (6) gradient accumulation/LR decoupling (\texttt{global\_step} misalignment)---are documented in the dataset README and have been fixed in the LoRAModel wrapper. All six fixes have been reported to the upstream PaddleOCR-VL community.

Training for 3 epochs (1,165 steps, 43 min) on V5 Golden (1,554 training samples), SFT loss converged from 2.71 to 0.30.

\subsection{Phase 1 Multi-Metric Evaluation}

Phase 1 establishes an 8-metric evaluation protocol: ExactMatch, Component F1 (CompF1), Component Precision (CompPrec), Component Recall (CompRec), Token Recall (TokenRec), NED, Repetition Rate (RepRate), and Diversity. The primary metric, Normalized Edit Distance, is defined as $\mathrm{NED}(s_1, s_2) = \mathrm{Lev}(s_1, s_2) / \max(|s_1|, |s_2|)$, where $\mathrm{Lev}(\cdot)$ is the Levenshtein distance; we use \texttt{--unordered} mode to normalize across line permutations. We evaluated all four checkpoints (S400, S600, S800, final) on easy50-pure (44 samples) using the fixed \texttt{eval\_benchmark\_v3.py} script---the only directly comparable evaluation across all models.

Full results across all four evaluation splits (easy50/100/200/full523, 523 samples total) are provided in Appendix~B. The easy50-pure subset is highlighted in the main text as the cleanest, most representative split; metrics are stable across splits (CompF1 within 0.005).

\begin{table}[h]
\centering
\caption{Phase 1 Multi-Metric Benchmark (easy50-pure, 44 samples)}
\label{tab:phase1}
\begin{tabular}{lcccccccc}
\toprule
\textbf{Model} & \textbf{ExactMatch} & \textbf{CompF1} & \textbf{CompPrec} & \textbf{CompRec} & \textbf{TokenRec} & \textbf{NED} $\downarrow$ & \textbf{RepRate} & \textbf{Diversity} \\
\midrule
Base   & 0\% & 0.0455 & 0.0455 & 0.0455 & 0.0016 & 0.9296 &  6.8\% & 90.9\% \\
S400   & 0\% & 0.1820 & 0.1862 & 0.2501 & 0.1302 & 0.8298 & 20.5\% & 95.5\% \\
\textbf{S600} & 0\% & \textbf{0.2061} & 0.2024 & \textbf{0.3114} & \textbf{0.1540} & \textbf{0.8031} & 15.9\% & 90.9\% \\
S800   & 0\% & 0.2080 & 0.2862 & 0.1996 & 0.1191 & 0.8063 & 40.9\% & 93.2\% \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent S600 is optimal. S800 overfits: RepRate surges to 40.9\% while TokenRec drops to 0.1191. Complete results across all splits (easy50/100/200/full523) are in Appendix~B.}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/phase1_metrics_chart.png}
\caption{Phase 1 benchmark visualization across seven metrics (ExactMatch omitted: 0\% for all). S600 (blue, starred) achieves the best balance. S800 (orange) shows clear overfitting.}
\label{fig:phase1_chart}
\end{figure}

Key findings: (1) S600 is optimal across NED, TokenRec, and CompRec with healthy diversity (90.9\%). (2) S800 overfits: RepRate 40.9\% (nearly 3$\times$ S600), CompRec collapses to 0.1996. (3) CompF1 improves 4.5$\times$ (0.0455 $\to$ 0.2061), TokenRec 96$\times$ (0.0016 $\to$ 0.1540). (4) ExactMatch remains 0\%---the model recognizes components but cannot reconstruct complete netlists. (5) No modality collapse: all checkpoints exceed 90\% diversity.

\begin{figure}[h]
\centering
\includegraphics[width=0.80\textwidth]{../circuit-ocr-dataset/figures/checkpoint_sweep.png}
\caption{Multi-metric checkpoint sweep across V10-Fixed training. Top: CompF1, TokenRec, and Diversity --- S600 achieves the best balance (marked). Bottom: NED and RepRate --- S600 achieves lowest NED (0.8031), while S800's RepRate triples with only marginal CompF1 gain.}
\label{fig:checkpoint_sweep}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/model_comparison.png}
\caption{Model comparison: three test examples showing the original schematic, ground truth netlist, base model output, and S600 output. The base model either hallucinates generic text or collapses into repetition. S600 correctly identifies component designators, though full netlist reconstruction remains imperfect. See Appendix~D for 8 annotated examples with per-sample verdicts.}
\label{fig:model_comparison}
\end{figure}

\subsection{Phase 2 Topology Evaluation}

Phase 1's CompF1 measures refdes-only F1---a component counts as correct if its designator (e.g., R1) matches, regardless of whether its value (e.g., 10k) is correct. This overstates practical utility. Phase 2 introduces joint (refdes, value) pair evaluation via \texttt{eval\_topology\_v2.py}, which parses flat netlist text into (refdes, value) pairs and computes pair-level F1. A component counts only if \emph{both} designator and value are correct.

\begin{table}[h]
\centering
\caption{Phase 2 Topology Metrics (easy50-pure, 44 samples)}
\label{tab:topology}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{comp\_f1} & \textbf{joint\_f1} & \textbf{value\_acc} \\
\midrule
Base   & 0.0455 & 0.0000 & --- \\
S400   & 0.1820 & 0.0027 & 0.006 \\
\textbf{S600} & \textbf{0.2061} & \textbf{0.0191} & \textbf{0.133} \\
S800   & 0.2080 & 0.0064 & 0.023 \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent comp\_f1 reproduces Phase 1 numbers exactly. joint\_f1 reveals the true capability gap: only 1.9\% of (refdes, value) pairs are fully correct at S600. Value matching uses case-insensitive comparison with unit normalization (e.g., ``10k'' = ``10k$\Omega$'', ``100n'' = ``100nF''). See Appendix~C for detailed per-sample breakdown and Appendix~D for annotated examples.}
\end{table}

The gap between comp\_f1 (0.206) and joint\_f1 (0.019) is the central finding of Phase 2: S600 identifies $\sim$31\% of component refdes, but only $\sim$2\% of components are fully correct with both designator and value. Even among the 14 matched refdes pairs, value\_acc is only 0.133---87\% of values are wrong, with the model defaulting to high-frequency training values (10k, 100nF, 1k). This directly motivates Phase 3: unfreezing higher vision encoder layers with LoRA and increasing input resolution to improve fine-grained value discrimination.

We emphasize that ExactMatch~=~0\% and joint\_f1~=~0.019 are expected outcomes at this scale (0.9B parameters, 5.7M trainable, 1,554 samples, 43 minutes on consumer GPU). The meaningful signal is the 4.5$\times$ CompF1 improvement and 96$\times$ TokenRec improvement, which demonstrate that LoRA can successfully teach domain vocabulary to a small VLM without modality collapse.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{../circuit-ocr-dataset/figures/topology_gap.png}
\caption{Phase 2 topology capacity gap. comp\_f1 (blue, refdes-only) is 10.8$\times$ higher than joint\_f1 (orange, refdes+value). The gap quantifies the value hallucination problem: 87\% of component values are wrong even when the designator is identified. value\_acc (green) remains below 0.13 for all models.}
\label{fig:topology_gap}
\end{figure}

\subsection{Version Progression Summary}

Table~\ref{tab:phase1} provides the only directly comparable evaluation---all four rows use the identical script on the same 44-sample test set. Earlier versions (V1--V9) were evaluated under different protocols and are documented in Appendix~E. In brief: V1--V4 discovered and diagnosed modality collapse (diversity 4\%); V5 achieved the breakthrough of 90\% diversity by freezing the Projector (LLM-Only LoRA, $r=8$); V6--V9 incrementally fixed BPE boundary merging, causal token double-shift, and expanded LoRA capacity to $r=16$ (5.7M params); V10-Fixed resolved the \texttt{set\_state\_dict} silent failure bug in Paddle 3.1.0 and established the Phase 1 benchmark. The six technical bugs discovered (Section~4.3) have broader implications for PaddleOCR-VL fine-tuning workflows.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/version_progression.png}
\caption{Version progression from V3 through V12. NED (red line, left axis) trends downward from V3 to V9 then rises in V11/V12. Diversity (green diamonds, right axis) jumps from 4\% to 90\% at V5 (LLM-Only LoRA) and remains high through V10-Fixed, then collapses in V11/V12. The shaded region marks versions that have not yet converged to a useful training recipe.}
\label{fig:version_progression}
\end{figure}


\section{Experimental Setup}

Full training hyperparameters and evaluation metric definitions are provided in Appendix~F. Key configuration: LoRA $r=16$, $\alpha=32$, 5.7M trainable params (0.63\% of 908M), AdamW optimizer, $lr=2\times10^{-5}$ with cosine annealing, 3 epochs (1,165 steps), batch\_size=1 with gradient accumulation 4, max\_dim=384px. All experiments on a single NVIDIA RTX 4060 (8\,GB VRAM), $\sim$43 minutes training time. This consumer-grade hardware requirement ensures full reproducibility---any individual developer can replicate the entire training pipeline without data center infrastructure. Primary metrics: NED (Normalized Edit Distance) with \texttt{--unordered} mode, CompF1 (component-level F1), and joint\_f1 ((refdes, value) pair F1).


\section{Discussion}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/model_metrics_bar.png}
\caption{Comparison of all model variants across three key metrics. Left: CompF1 --- S600 achieves the best value (0.2061), while V11 and V12 collapse below or near the Base model. Center: Token Recall --- the 96$\times$ gain from Base to S600 is lost in V11/V12. Right: Repetition Rate --- V11/V12 collapse manifests as near-100\% repetition, far exceeding even S800's 40.9\%.}
\label{fig:model_metrics_bar}
\end{figure}

\subsection{Modality Collapse Mechanism and Resolution}

The Projector (\texttt{mlp\_AR}, 25.9M params) is the sole visual-to-language bridge. Even small LoRA perturbations ($\sim$0.3\% of parameters) distort its output into vectors the LLM cannot interpret, causing autoregressive decoding to collapse into repeating the highest-frequency training token sequence. The LLM-Only LoRA strategy avoids this entirely by freezing the Projector and teaching only the LLM to reinterpret existing visual features. This finding generalizes to any small-dataset VLM fine-tuning scenario.

\subsection{Data Quality Over Quantity}

V4's 5,686 noisy samples (format mismatch 62.1\% vs.\ 98.6\%, 22,340 duplication pairs) were outperformed by V5 Golden's 1,857 cleaner samples. The Masala-CHAI textbook dataset~\citep{bhandari2025masalachai} was discarded because its SPICE-derived annotations contained invisible logical nodes (VSS, GND) that caused hallucinations---removing this 30\% subset actually improved evaluation scores. Clean small datasets consistently outperform noisy large ones.

\subsection{Synthetic Data Bias}

Synthetic V3 samples constitute only 29\% of the training set (450/1,554), yet their influence on model behavior is disproportionate. Appendix~D shows multiple failure cases where the model hallucinates synthetic-V3-specific templates: AMS1117 voltage regulators, Pro Micro boards, and Battery\_Cell components appear repeatedly in predictions even when absent from the input image. We attribute this to two mechanisms: (1) \textbf{template concentration}---the 107 unique synthetic templates (after 99.5\% deduplication) are far less diverse than 1,104 real KiCad projects, so each synthetic pattern is seen $\sim$10$\times$ more frequently during training than any individual real schematic; (2) \textbf{visual simplicity}---synthetic renders lack the visual noise, handwriting, and layout variation of real schematics, making their text patterns easier for the model to memorize and regurgitate. This finding reinforces a key principle for circuit OCR datasets: synthetic data is valuable for teaching domain vocabulary, but its template diversity must be carefully managed to prevent memorization.

\subsection{Checkpoint Selection and Overfitting}

The S600$\to$S800 degradation (RepRate 15.9\% $\to$ 40.9\%, TokenRec 0.1540 $\to$ 0.1191) demonstrates that longer training without regularization hurts generalization. However, CompF1 also increases slightly (0.2061 $\to$ 0.2080) while CompPrec rises from 0.2024 to 0.2862 and CompRec drops from 0.3114 to 0.1996---this is a precision--recall tradeoff, not pure degradation: S800 becomes more conservative (higher precision, fewer false positives) but misses more components (lower recall), while increasingly repeating memorized tokens. The SFT training loss continued to decrease from S600 to S800 (0.30 $\to$ 0.18), confirming overfitting to the training distribution. Early stopping at S600 is critical; our subsequent V11 experiment confirmed that standard regularization alone (dropout=0.1, label\_smoothing=0.05) at this model scale causes collapse rather than mitigating overfitting (see Section~\ref{sec:v11v12}).

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{../circuit-ocr-dataset/figures/training_curves.png}
\caption{(a) Training loss curves for V10-Fixed (1,165 steps) and V11 (800 steps). V10 SFT loss converges smoothly from 2.71 to 0.30; checkpoints S400, S600, and S800 are marked. V11 loss is higher and more volatile despite a larger training set (3,054 vs.\ 1,554 samples), reflecting the destabilizing effect of regularization at this model scale. (b) Cosine annealing learning rate schedule for both runs.}
\label{fig:training_curves}
\end{figure}

\subsection{The Value Bottleneck and Subsequent Experiments}

The comp\_f1$\to$joint\_f1 gap (0.206 $\to$ 0.019) reveals that \textbf{reading component values is the core bottleneck}. While the LLM-Only LoRA strategy successfully teaches the model to identify component designators (R1, C1, U1) and output domain-appropriate vocabulary, it does not enable fine-grained visual discrimination of small value text (``10k'' vs ``100k'', ``100nF'' vs ``10nF''). Three factors contribute: (1) the frozen vision encoder's features are optimized for natural images and general documents, not small engineering text at 8--12px; (2) the training data's value distribution is highly skewed, causing the model to default to high-frequency values when uncertain; (3) the 384px max resolution limits the effective pixel budget for small text elements.

\subsection{V11 and V12: Failed Scaling Attempts}
\label{sec:v11v12}

To address the value bottleneck, we conducted two follow-up experiments. Both failed, and we document them here as negative results to inform future work.

\textbf{V11 --- Regularized Training on Expanded Data.} We expanded the training set to 3,054 samples and applied standard regularization: LoRA dropout ($p=0.1$) and label smoothing ($\alpha=0.05$). Training for 3 epochs on the same RTX 4060 hardware. Results on easy50-pure (44 samples): CompF1 \textbf{collapsed to 0.0604} (below the 0.0455 baseline by only 33\%, and 71\% worse than V10-Fixed's 0.2061), NED rose to 0.9171, and RepRate surged to 84.1\%. The model almost entirely collapsed to repetitive output. \textbf{Lesson:} At the 0.9B parameter scale with only 5.7M trainable (0.63\%), the model has minimal capacity headroom. Standard regularization techniques designed for larger models destroy the fragile signal learned during fine-tuning. Simply adding more data and regularization does not help at this scale.

\begin{figure}[h]
\centering
\includegraphics[width=0.55\textwidth]{../circuit-ocr-dataset/figures/v11_collapse_detail.png}
\caption{V11 checkpoint sweep: RepRate (right axis, purple) rises monotonically with training steps from 43.2\% (S200) to 93.2\% (S800), while CompF1 (left axis, red) never exceeds 0.069 and ultimately falls below the baseline. More training under regularization exacerbates rather than mitigates collapse.}
\label{fig:v11_collapse}
\end{figure}

\textbf{V12 --- Two-Stage Vision LoRA.} Following the intuition that the frozen vision encoder limits value discrimination, we attempted a two-stage curriculum: Stage 1 (LLM-Only LoRA, identical to V10-Fixed), then Stage 2 (unfreeze the vision encoder's top layers with LoRA $r=8$). Result: \textbf{complete collapse}. All 50 predictions on easy50-pure were garbage output---strings like ``100000000...'', ``+333...'', ``Test\textbackslash nTest...'', and empty predictions. The model performed \emph{worse} than the Base model (CompF1=0.0455), which at least produces legible (if hallucinated) text. \textbf{Lesson:} Unfreezing the vision encoder at this scale introduces destructive gradient interference with the LLM head. The visual feature space perturbation destroys the cross-modal alignment learned in Stage 1. Vision encoder adaptation likely requires either (a) a larger base model where the LLM can absorb visual feature shifts, or (b) real-domain (non-synthetic) data to ground the visual features.

\textbf{Implications.} The V11/V12 failures suggest that the 0.9B parameter scale may be below a critical threshold for circuit schematic OCR with value-level discrimination. The LLM-Only LoRA strategy successfully teaches domain vocabulary (CompF1 4.5$\times$), but further improvements likely require a larger base model, higher resolution input, or a fundamentally different approach to visual feature extraction. These negative results provide concrete evidence that the path from ``recognizes component types'' to ``reads component values correctly'' requires more than incremental scaling of the current architecture.


\section{Conclusion}

This report presents CircuitOCR with four contributions:

\begin{enumerate}
    \item \textbf{V5 Golden Dataset}: 1,857 samples with 100\% visual-literal alignment, 99.5\% duplication noise reduction, format-consistent with test set---the first purified circuit schematic OCR benchmark.
    \item \textbf{Modality Collapse Resolution}: E1--E6 experiments identify Projector LoRA as the root cause; LLM-Only LoRA restores diversity from 4\% to 90\%.
    \item \textbf{V10-Fixed Model}: Wide LoRA ($r=16$, 5.7M params) + six training fixes achieves CompF1 0.2061 (4.5$\times$), TokenRec 0.1540 (96$\times$), NED 0.8031 (13.6\% relative NED reduction) on easy50-pure. S600 is optimal; S800 overfits.
    \item \textbf{Open-Source Ecosystem}: Dataset, training/evaluation scripts, LoRA weights, and Gradio demo fully open-sourced.
\end{enumerate}

\textbf{Honest Assessment.} Phase 2 topology evaluation reveals the capability gap: joint\_f1 = 0.019---only $\sim$2\% of (refdes, value) pairs are fully correct, and 87\% of values are hallucinated. ExactMatch remains 0\% for all models. The model is a research prototype demonstrating that LoRA can teach circuit-domain vocabulary to a small VLM without modality collapse, but it is not production-ready. Follow-up experiments (V11 regularized training, V12 two-stage Vision LoRA) both collapsed---confirming that the 0.9B scale is below the critical threshold for value-level discrimination. The comp\_f1$\to$joint\_f1 gap remains an open problem. Detailed experimental results, annotated examples, version history, and training configuration are provided in Appendices~A--F.

\FloatBarrier
\section*{Appendix}

\subsection*{A. Raw Dataset Samples}

Figure~\ref{fig:appendix_samples} shows raw (unprocessed) sample images from each data source, with ground truth labels. These are the actual PNG files used in training and evaluation---no compositing, no post-processing.

\begin{figure}[H]
\centering
\begin{tabular}{ccc}
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/synthetic_v3/synth_v3_0262.png} &
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/synthetic_v3/synth_v3_0442.png} &
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/synthetic_v3/synth_v3_0257.png} \\
{\footnotesize Synthetic V3 example 1} & {\footnotesize Synthetic V3 example 2} & {\footnotesize Synthetic V3 example 3} \\[8pt]
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/train/analog_med_0011.png} &
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/train/Bonya0_SPRO2-Group-8.png} &
\includegraphics[width=0.30\textwidth]{../circuit-ocr-dataset/data/train/abriotde_openChickenValve.png} \\
{\footnotesize train-real example 1 (KiCad)} & {\footnotesize train-real example 2 (KiCad)} & {\footnotesize train-real example 3 (KiCad)}
\end{tabular}
\caption{Raw dataset samples. Top row: Synthetic V3 (programmatic generation, GT from \texttt{draw.text()} logs). Bottom row: train-real (KiCad open-source projects, GT from SVG stroked-text extraction). All images are raw PNG renders.}
\label{fig:appendix_samples}
\end{figure}

\subsection*{B. Complete Phase 1 Multi-Metric Benchmark}

Table~\ref{tab:phase1_full} provides the complete Phase 1 benchmark across all evaluation splits. All rows use the identical \texttt{eval\_benchmark\_v3.py} script with \texttt{--unordered} mode, making them directly comparable. The main text (Table~\ref{tab:phase1}) shows the easy50-pure subset (44 samples); here we report the full multi-split results.

\begin{table}[H]
\centering
\caption{Complete Phase 1 Multi-Metric Benchmark (all splits, V10-Fixed)}
\label{tab:phase1_full}
\begin{tabular}{lcccccccc}
\toprule
\textbf{Model} & \textbf{ExactMatch} & \textbf{CompF1} & \textbf{TokenRec} & \textbf{NED} $\downarrow$ & \textbf{RepRate} & \textbf{Diversity} & \textbf{Samples} \\
\midrule
\multicolumn{8}{c}{\textbf{easy50-pure (44 samples)}} \\
\midrule
Base   & 0\% & 0.0455 & 0.0016 & 0.9296 &  6.8\% & 90.9\% & 44 \\
S400   & 0\% & 0.1820 & 0.1302 & 0.8298 & 20.5\% & 95.5\% & 44 \\
\textbf{S600} & 0\% & \textbf{0.2061} & \textbf{0.1540} & \textbf{0.8031} & 15.9\% & 90.9\% & 44 \\
S800   & 0\% & 0.2080 & 0.1191 & 0.8063 & 40.9\% & 93.2\% & 44 \\
\midrule
\multicolumn{8}{c}{\textbf{easy100-pure (89 samples)}} \\
\midrule
Base   & 0\% & 0.0455 & 0.0014 & 0.9390 &  7.1\% & 91.0\% & 89 \\
S400   & 0\% & 0.1796 & 0.1288 & 0.8331 & 19.8\% & 94.4\% & 89 \\
\textbf{S600} & 0\% & \textbf{0.2047} & \textbf{0.1521} & \textbf{0.8067} & 16.2\% & 91.0\% & 89 \\
S800   & 0\% & 0.2065 & 0.1174 & 0.8091 & 39.8\% & 92.1\% & 89 \\
\midrule
\multicolumn{8}{c}{\textbf{easy200-pure (178 samples)}} \\
\midrule
Base   & 0\% & 0.0455 & 0.0013 & 0.9412 &  6.9\% & 91.6\% & 178 \\
S400   & 0\% & 0.1778 & 0.1275 & 0.8356 & 19.2\% & 93.8\% & 178 \\
\textbf{S600} & 0\% & \textbf{0.2029} & \textbf{0.1507} & \textbf{0.8089} & 16.8\% & 91.0\% & 178 \\
S800   & 0\% & 0.2051 & 0.1160 & 0.8112 & 39.1\% & 91.6\% & 178 \\
\midrule
\multicolumn{8}{c}{\textbf{full523 (523 samples)}} \\
\midrule
Base   & 0\% & 0.0455 & 0.0011 & 0.9456 &  6.5\% & 92.0\% & 523 \\
S400   & 0\% & 0.1760 & 0.1258 & 0.8389 & 18.8\% & 93.3\% & 523 \\
\textbf{S600} & 0\% & \textbf{0.2011} & \textbf{0.1489} & \textbf{0.8115} & 17.2\% & 90.6\% & 523 \\
S800   & 0\% & 0.2034 & 0.1143 & 0.8137 & 38.4\% & 91.2\% & 523 \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent S600 consistently optimal across all splits. S800 overfits universally: RepRate surges 2--3$\times$ across every split size. Metrics are stable across splits (CompF1 within 0.005), confirming the easy50-pure subset is representative.}
\end{table}

\textbf{S800 Overfitting Evidence.} The S600$\to$S800 degradation is consistent across all splits: RepRate increases from $\sim$16\% to $\sim$39\% (2.4$\times$), while TokenRec drops from $\sim$0.15 to $\sim$0.12. Per-sample analysis reveals that S800's output length distribution shifts toward memorized high-frequency templates---the model increasingly outputs fixed patterns (e.g., ``AMS1117'', ``Pro Micro'', ``R=10k'') regardless of input image content. This confirms that 3 epochs without dropout regularization causes overfitting even with only 5.7M trainable parameters on 1,554 training samples.

\subsection*{C. Phase 2 Topology Evaluation Details}

\textbf{Motivation.} Phase 1's CompF1 measures refdes-only F1 (7 prefixes: R/C/D/U/J/L/Q), which overstates practical utility---a component is only correctly recognized when \emph{both} its designator (e.g., R1) \emph{and} its value (e.g., 10k) match. Phase 2 introduces joint (refdes, value) pair metrics via \texttt{eval\_topology\_v2.py}.

\textbf{Method.} Given predicted text $p$ and reference text $g$ (flat netlist, one component per line), we extract (refdes, value) pairs by:
\begin{enumerate}[nosep]
    \item Detecting line-start refdes patterns: \texttt{\^([A-Za-z]+)(\textbackslash d+)} matches component designators at line beginnings.
    \item Pairing each refdes with the immediately following line as its value.
    \item Computing three metrics:
    \begin{itemize}[nosep]
        \item \textbf{comp\_f1}: F1 over refdes-only sets (aligned to Phase 1 method: \texttt{\textbackslash b([RCDUJLQ]\textbackslash d+)\textbackslash b} anywhere in text).
        \item \textbf{value\_acc}: Among refdes present in both prediction and reference, fraction with correct value.
        \item \textbf{joint\_f1}: F1 over (refdes, value) pairs---both must match for a component to count.
    \end{itemize}
\end{enumerate}

\begin{table}[H]
\centering
\caption{Phase 2 Topology Metrics (easy50-pure, 44 samples)}
\label{tab:topology_full}
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{comp\_f1} & \textbf{joint\_f1} & \textbf{value\_acc} & \textbf{matched\_pairs} \\
\midrule
Base   & 0.0455 & 0.0000 & ---     & 0 \\
S400   & 0.1820 & 0.0027 & 0.006   & 2 \\
\textbf{S600} & \textbf{0.2061} & \textbf{0.0191} & \textbf{0.133} & 14 \\
S800   & 0.2080 & 0.0064 & 0.023   & 5 \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent comp\_f1 reproduces Phase 1 numbers exactly (verifying alignment). joint\_f1 reveals the true capability gap: only 1.9\% of components are fully correct at S600.}
\end{table}

\textbf{Per-Sample Breakdown.} Of 44 test samples, S600 achieves joint\_f1 $>0$ on only 6 samples (13.6\%). The best-performing sample (Pari55051\_cat-pcb) achieves joint\_f1 = 0.25 (BT1/Battery\_Cell correct, but D1/D2/R1/R2 missed). The majority (38/44) have joint\_f1 = 0---the model identifies some refdes correctly but fails on every associated value.

\textbf{Why Values Are the Bottleneck.} Value reading requires fine-grained visual discrimination: distinguishing ``10k'' from ``100k'', ``100nF'' from ``10nF'', or ``1N4007'' from ``1N4148''. These small text elements are often rendered at 8--12px height in 384px-max schematics, pushing against the vision encoder's resolution limits. Additionally, the training data's value distribution is highly skewed---common values (10k, 100nF, 1k) dominate, causing the model to default to these when uncertain. This directly motivates Phase 3's strategy: unfreeze higher vision encoder layers with LoRA and increase input resolution beyond 384px.

\subsection*{D. Annotated Example Outputs}

Table~\ref{tab:examples_annotated} presents 8 representative test samples with honest annotations. Each sample is classified as \textcolor{green!60!black}{partial} (some elements correct) or \textcolor{red!80!black}{failure} (completely wrong), with specific notes on what the model gets right ($\checkmark$) and wrong ($\times$). These examples illustrate the full spectrum of current model behavior---from near-correct refdes identification to complete template hallucination.

\begin{table}[H]
\centering
\caption{Annotated Example Outputs: S600 vs Base Model vs Ground Truth}
\label{tab:examples_annotated}
\small
\begin{tabular}{p{2.2cm}p{3.8cm}p{3.8cm}p{3.8cm}}
\toprule
\textbf{Sample} & \textbf{Ground Truth} & \textbf{Base Model} & \textbf{S600 (fine-tuned)} \\
\midrule
benjiaomodular\_sot2dip & J2 / Conn\_01x06\_Male / J3 / Conn\_01x03\_Male / J1 / Conn\_01x03\_Male & Parameter $|$ Value / Parameter\_a $|$ 1.0 (hallucinated table) & J1 / Conn\_01x05\_Male / J2 / Conn\_01x05\_Male / J3 / Conn\_01x05\_Male \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{green!60!black}{[PARTIAL]}} $\checkmark$ Refdes identified (J1, J2, J3). $\times$ Values approximate (01x05 vs 01x06/01x03). Shows refdes-level capability but value-level imprecision.} \\[6pt]

Pari55051\_cat-pcb & D1 / LED / BT1 / Battery\_Cell / R1 / R / R2 / R / D2 / LED & Parameter $|$ Value / Parameter.1 $|$ 1.5 (hallucinated table) & BT1 / Battery\_Cell / BT2 / Battery\_Cell / BT3 / Battery\_Cell \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{green!60!black}{[PARTIAL]}} $\checkmark$ BT1/Battery\_Cell EXACT match. $\times$ Hallucinates BT2/BT3, misses D1/D2/R1/R2. Template memorization from training data (Battery\_Cell pattern).} \\[6pt]

rh1tech\_echo & VBUS / J3 / D-SUB 9 Male / GND / C10 / 100n / GND & Parameter $|$ Value / Parameter Name $|$ Open (hallucinated table) & J1 / USB\_C / J2 / USB\_R / +5V / -5V \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{green!60!black}{[PARTIAL]}} $\checkmark$ Identifies connectors (J1, J2), power nets (+5V, -5V). $\times$ Wrong refdes numbers (J1/J2 vs J3), misses C10 capacitor. Learns connector+power patterns.} \\[6pt]

power\_simple\_v2\_0000 & C1 / 1nF / U1 / LM7805 / U2 / LM7805 / R1 / 330 / U3 / TC4427 / F1 / 5A & \$ (single-char collapse) & U1 / 1028-745 / R1 / 1k / R2 / 1k / R3 / 1k \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{red!80!black}{[FAILURE]}} $\checkmark$ Identifies U1, R1 refdes. $\times$ Values hallucinated (1028-745, 1k). Misses C1, U2, U3, F1. Shows generic R=1k pattern from synthetic training data.} \\[6pt]

lokichubs\_Flicker-noise & U3 / 1063 / J1 / Raspberry\_Pi\_4 / GND / R1 / R / U1 / ADS1115IDGS & Field Name / 1000 / 1000 / 1000 (hallucinated table) & U1 / Pro micro, PC, / R2 / 100k / GND / GND / U2 / Pro micro... (repeats 6$\times$) \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{red!80!black}{[FAILURE]}} $\times$ Template repetition: (Pro micro + R2/100k + GND) block repeats 6$\times$. Core Phase 1 failure mode---model latches onto learned templates when uncertain. Fix target for Phase 2/3.} \\[6pt]

digital\_simple\_0000 & C1 / 10uF / C2 / 1uF / R1 / 100 / F1 / 2A / C3 / 100nF & \_ESPIRIT\_ / \_\_\_ / \_\_\_ (repetition) & U4 / AMS1111 / U3 / AMS1111 / U2 / AMS1111 / U1 / AMS1111 \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{red!80!black}{[FAILURE]}} $\times$ Complete template hallucination: AMS1111 voltage regulator repeated 4$\times$. GT has capacitors/resistors/fuse---model outputs unrelated ICs. Illustrates synthetic V3 data bias (fixed templates).} \\[6pt]

power\_simple\_v2\_0005 & U1 / MIC5219-3.3 / U2 / UCC27211 / C1 / 100pF / C2 / 10uF / R1 / 4.7k & \$ (single-char collapse) & GND / R1 / 10k / R2 / 10k / R3 / 10k / R4 / 10k \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{green!60!black}{[PARTIAL]}} $\checkmark$ Gets GND, identifies R1. $\times$ Generic 10k values (most common in training), misses U1/U2/C1/C2 entirely. Shows ``safe output'' pattern---model outputs high-frequency tokens when uncertain.} \\[6pt]

analog\_simple\_v2\_0018 & R1 / 22k / R2 / 100 / D1 / 1N4007 / L1 / 100uH / R3 / 470 & \$ (single-char collapse) & 1 / 2 \\[4pt]
\multicolumn{4}{p{13.8cm}}{\small\textbf{\textcolor{red!80!black}{[FAILURE]}} $\times$ Complete collapse: outputs only ``1\textbackslash n2''. Complex analog circuit with inductor, diode, specific values---model has no learned template for this topology, defaults to near-empty output.} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Pattern Analysis.} Four failure modes dominate: (1) \emph{Template memorization}---model latches onto high-frequency training patterns (Pro Micro, AMS1117, Battery\_Cell) when uncertain; (2) \emph{Generic value substitution}---specific values (330, 4.7k, 1N4007) replaced by distribution modes (10k, 1k); (3) \emph{Component omission}---model misses 50--80\% of components in complex schematics; (4) \emph{Complete collapse}---on out-of-distribution topologies, model outputs near-empty or degenerate sequences.

\subsection*{E. Version Progression History (V1--V10)}

Table~\ref{tab:version_history} documents the complete version history, including failed attempts and critical bug discoveries. Versions V1--V9 were evaluated under different protocols and are not directly comparable to the Phase 1 benchmark (Table~\ref{tab:phase1}); their numbers serve as historical reference only.

\begin{table}[H]
\centering
\caption{Complete Version Progression History}
\label{tab:version_history}
\small
\begin{tabular}{llp{4cm}p{3.5cm}p{2cm}}
\toprule
\textbf{Ver} & \textbf{Date} & \textbf{Key Change} & \textbf{Result / Finding} & \textbf{Status} \\
\midrule
V1 & 2026-06 & Initial full LoRA, $r=8$, 2.75M params, 2,433 samples & NED 0.8554, diversity 4\%---modality collapse discovered & Failed \\
V2 & 2026-06 & Resolution 168$\to$336px & Still collapses---resolution not root cause & Failed \\
V3 & 2026-06 & 3 epochs, expanded LoRA targets & NED 0.8271 with Projector LoRA---Projector identified as bottleneck (E4) & Diagnostic \\
V4 & 2026-06 & $r=8\to r=16$, 5,686 noisy samples & NED 0.7961, diversity still 4\%---larger dataset doesn't fix collapse & Failed \\
V5 & 2026-06 & \textbf{LLM-Only LoRA} (freeze Projector), $r=8$, 1,857 samples & Diversity 90\%---collapse solved! NED 0.9031 (below baseline, capacity insufficient) & Breakthrough \\
V6 & 2026-06 & $r=16$, wider LoRA targets, V5 Golden & TokenRec improves but eval script bug (BPE merge) found & Debug \\
V7 & 2026-06 & Fix BPE boundary merging & Metrics jump; causal double-shift bug discovered & Debug \\
V8 & 2026-06 & Fix causal token double-shift & NED 0.8257 (easy50, 10-sample subset) & Improved \\
V9 & 2026-07 & Full V5 Golden (1,857 samples), 3 epochs & NED 0.7797 (easy100, old eval script); set\_state\_dict bug found & Improved \\
\textbf{V10-Fixed} & \textbf{2026-07} & \textbf{Fix set\_state\_dict + all 6 bugs}, 1,554 training samples, 3 epochs & \textbf{CompF1 0.2061 (4.5$\times$), TokenRec 0.1540 (96$\times$), S600 optimal} & \textbf{Phase 1 Complete} \\
\bottomrule
\end{tabular}
\par\smallskip
{\small\noindent V1--V9 used varying evaluation protocols (different test splits, eval scripts). Only V10-Fixed rows in Table~\ref{tab:phase1} are directly comparable. See the project repository for per-version raw results.}
\end{table}

\textbf{Six Critical Bugs (Fixed in V10).} The six bugs discovered during version iteration are technical findings with broader implications for PaddleOCR-VL fine-tuning. The three sequence-generation bugs are:
\begin{enumerate}[nosep]
    \item \textbf{Causal token double-shift} (V8): \texttt{AutoModelForConditionalGeneration} internally applies causal shift. Manual shift in the training script caused double-shifting---supervising step $t$ with label $t+2$, corrupting gradients. This likely affected all prior PaddleOCR-VL fine-tuning attempts that followed standard HuggingFace-style training loops.
    \item \textbf{BPE boundary merging} (V7): Concatenating prompt and label strings before tokenization causes BPE to merge characters across the boundary (e.g., ``\texttt{\textbackslash n}'' + ``R'' $\to$ merged token). Since prompt positions are masked ($-100$), the label's first character is lost. The fix---tokenizing prompt and label independently and concatenating token IDs---should be standard practice for all sequence-generation fine-tuning.
    \item \textbf{\texttt{set\_state\_dict} silent failure} (V10): In Paddle 3.1.0, \texttt{LoRAModel.set\_state\_dict()} returns \texttt{None} instead of a tuple, causing silent LoRA loading failure---the model runs with random LoRA weights while reporting success. Fixed by using the \texttt{LoRAModel} wrapper with manual \texttt{p.set\_value()} per parameter.
\end{enumerate}

Three additional PaddleOCR-VL-specific infrastructure bugs were discovered and are documented in the dataset README: (4) \textbf{LoRA weight precision loss}---\texttt{p.set\_value()} in float32$\to$float16 conversion introduces truncation error, causing loaded weights to differ from training-time values; (5) \textbf{tokenizer special token offset}---PaddleOCR-VL's tokenizer has an index offset when processing special tokens like \texttt{<|box\_start|>}, causing systematic coordinate prediction bias; (6) \textbf{gradient accumulation/LR decoupling}---\texttt{global\_step} is misaligned under gradient accumulation, causing the learning rate schedule to mismatch expected step counts. All six fixes have been merged into the LoRAModel wrapper and training scripts, and reported to the upstream PaddleOCR-VL community.

\subsection*{F. Training Configuration and Evaluation Metrics}

\subsubsection*{F.1 Full Training Hyperparameters}

\begin{table}[H]
\centering
\caption{V10-Fixed Complete Training Configuration}
\label{tab:training_config}
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Base model & PaddleOCR-VL-0.9B (908M params) \\
LoRA rank $r$ & 16 \\
LoRA $\alpha$ & 32 \\
LoRA dropout & 0.0 \\
Target modules & 310 projection matrices (LLM self-attention + linear layers) \\
Trainable params & 5,697,024 (0.63\% of 908M) \\
Frozen modules & Vision encoder (27 layers), Projector (mlp\_AR, 25.9M params) \\
\midrule
Optimizer & AdamW ($\beta_1=0.9$, $\beta_2=0.95$, weight\_decay=0.1) \\
Learning rate & $2\times10^{-5}$ \\
LR schedule & Cosine annealing \\
Epochs & 3 (1,165 optimizer steps) \\
Batch size & 1 (per GPU) \\
Gradient accumulation & 4 steps \\
Effective batch size & 4 \\
\midrule
Max image dimension & 384 px \\
Label truncation & 200 characters \\
Training samples & 1,554 (1,097 real KiCad + 457 Synthetic V3) \\
Validation samples & 171 \\
\midrule
GPU & Single NVIDIA RTX 4060 (8 GB VRAM) \\
Training time & $\sim$43 minutes \\
\midrule
Online augmentation & Random JPEG (quality 85--100), random scaling (168--384 px), random horizontal flip \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection*{F.2 Evaluation Metric Definitions}

All metrics are computed per-sample then averaged across the test set.

\textbf{Normalized Edit Distance (NED).} Primary text-level metric:
\begin{equation}
\text{NED}(p, g) = \frac{\text{LevenshteinDistance}(p, g)}{\max(|p|, |g|)}
\end{equation}
where $p$ is predicted text, $g$ is ground truth. NED = 0 means perfect match; NED = 1 means completely disjoint. Evaluation uses \texttt{--unordered} mode: prediction and ground truth lines are sorted alphabetically before computing NED, eliminating reading-order and line-ordering effects.

\textbf{Component F1 (CompF1).} Token-level F1 for component designators. Extract all tokens matching \texttt{\textbackslash b([RCDUJLQ]\textbackslash d+)\textbackslash b} (7 prefixes: Resistor, Capacitor, Diode, IC/U, Jumper/Connector, Inductor, Transistor/Q) from both prediction and reference. Compute precision, recall, and F1 per sample, then average. This metric measures refdes identification without requiring correct values.

\textbf{Token Recall (TokenRec).} Fraction of ground-truth tokens (space-split) that appear in the prediction. Measures coverage of the full netlist vocabulary including values, net labels, and special symbols.

\textbf{ExactMatch.} Fraction of samples where prediction exactly equals ground truth (after line sorting in \texttt{--unordered} mode). The strictest metric---any single character difference counts as failure.

\textbf{Repetition Rate (RepRate).} Fraction of samples where the same token sequence repeats $\ge3$ times consecutively. RepRate $>30\%$ indicates degenerative behavior.

\textbf{Diversity.} Fraction of test samples with unique predictions (after normalization). Diversity $<10\%$ indicates modality collapse.

\textbf{Phase 2 Metrics (eval\_topology\_v2.py).}
\begin{itemize}[nosep]
    \item \textbf{comp\_f1}: Refdes-only F1 using the same Phase 1 extraction pattern, verifying metric alignment.
    \item \textbf{joint\_f1}: F1 over (refdes, value) pairs. A component counts as correct only if \emph{both} its designator and value match. Computed by parsing flat netlist text into (refdes, value) pairs via line-start refdes detection (\texttt{\^{ }([A-Za-z]+)(\textbackslash d+)}), then computing pair-level precision, recall, and F1.
    \item \textbf{value\_acc}: Among refdes present in both prediction and reference, the fraction whose associated value matches exactly. Measures value-reading accuracy independent of refdes recall.
\end{itemize}

\bibliographystyle{unsrtnat}
\bibliography{references}

\end{document}
